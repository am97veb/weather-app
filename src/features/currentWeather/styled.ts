import styled from "styled-components";

export const Wrapper =  styled.div`
    max-width: 1500px;
    margin: 0 auto;
    display: flex;
    padding-top: 50px;
`;

export const CityList = styled.ul`
    padding-left: 0;
    display: flex;
    flex-wrap: wrap;
    gap:25px;
`;
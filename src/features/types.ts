export interface StyledComponendsProps {
    forecastView?: boolean;
    minimumTemperature?: boolean;
    smallTile?: boolean;
    mediumTile?: boolean;
    bigTile?: boolean;
    gridArea?: string;
}